<!doctype html>
<html>

    <% include ./partials/header %>

    <body>

        <div class="container content">
            <div class="col-xs-12 text-center">
                <h1 class="title">Plans tonight?</h1>
                <i class="fa fa-map-marker fa-3x"></i>
                <i class="fa fa-cab fa-3x"></i>
                <i class="fa fa-glass fa-3x"></i>
                <p>See which bars are hoppin' tonight and RSVP ahead of time!</p>
                <p>Remember: take a cab and drink responsible.</p>
            </div>

            <div class="col-xs-12">
                <form class="form-inline" id="target" action="/" method="post" enctype="application/x-www-form-urlencoded">
                    <div class="form-group col-xs-10">
                        <input type="text" class="form-control" name="location" id="location" placeholder="WHERE YOU AT" required>
                    </div>
                    <button class="btn btn-default col-xs-2">GO</button>
                </form>
            </div>

            <% if (displayResults) { %>
                <div class="col-xs-12 barContent">
                    <table class="table table-striped table-bordered" style="margin-top: 20px;">
                        <thead>
                        <tr>
                            <td>&nbsp;</td>
                            <td><strong>Name</strong></td>
                            <td><strong>Rating</strong></td>
                            <td><strong># Reviews</strong></td>
                            <td>&nbsp;</td>
                        </tr>
                        </thead>

                        <tbody>
                        <% for (var i = 0; i < results.businesses.length; i++) { %>
                        <tr>
                            <td><img src='<%- results.businesses[i].image_url %>' /></td>
                            <td><%- results.businesses[i].name %></td>
                            <td><%- results.businesses[i].rating %><br><img src='<%- results.businesses[i].rating_img_url %>' /></td>
                            <td><%- results.businesses[i].review_count %></td>
                            <td><br /><a href="/auth/facebook"><button class="btn btn-default" onclick="clickGoing('<%- results.businesses[i].name %>')"><span class="count">0 </span>GOING</button></a></td>
                        </tr>
                        <% } %>

                        </tbody>
                    </table>

                </div>
            <% } %>

        </div>

        <% include ./partials/scripts %>

        <script>
            function clickGoing(barName) {

            }
        </script>
    </body>
</html>